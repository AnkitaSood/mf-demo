{
  "name": "mf-demo",
  "version": "0.0.0",
  "private": true,
  "description": "Module Federation 2 demo: Angular 21 shell + React 19 + Vue 3 remotes",
  "scripts": {
    "start:shell": "npm run start --prefix ng-shell",
    "start:react": "npm run dev --prefix react-app",
    "start:vue": "npm run dev --prefix vue-app",
    "dev": "concurrently \"npm run start:shell\" \"npm run start:react\" \"npm run start:vue\"",
    "install:all": "npm install --prefix ng-shell && npm install --prefix react-app && npm install --prefix vue-app"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
